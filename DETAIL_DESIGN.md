# 詳細設計書

## 1. システム概要
- 需要予測データと在庫情報を参照し、発注量を計算後、車両台数と積載を最適化するバッチシステムをPythonで実装する。
- データはPostgreSQLに格納し、ETL処理はPandasで行う。最適化にはPuLPを使用。

## 2. モジュール構成
1. **ETLモジュール**
   - 各種CSVや外部システムからのデータを取得し、DWHスキーマにロードする。
   - 毎日実行し、差分更新を行う。ログは実行日時・件数・エラー内容を記録。
2. **発注計算モジュール**
   - 入力: 需要予測、現在庫、リードタイム、安全在庫、パレット情報。
   - 処理: 予測値と在庫を参照し、リードタイム後の在庫が安全在庫以上になる発注量を計算。目標在庫日数から上限を決定し、パレット単位に丸める。
   - 出力: 商品別の発注提案テーブルに保存。
3. **車両最適化モジュール**
   - 入力: 発注計算結果、車両マスタ、既定ルート情報。
   - 処理: PuLPで整数計画を定式化。目的関数は台数最小化と積載率向上の重み付け。制約には車両容量、積み付け制約、配送スケジュールを組み込む。
   - 出力: 日次便別の積載割当テーブル。
4. **ダッシュボード**
   - FastAPIとJinja2で実装。発注量・配送計画の一覧、履歴、修正依頼フォームを提供。
   - ユーザー権限に応じて編集可否を制御。

## 3. データベース設計 (論理モデル)
- **products**(product_id PK, name, size, weight, pallet_qty)
- **inventory_history**(product_id FK, date, quantity)
- **forecast**(product_id FK, date, qty, type)
- **orders**(order_id PK, product_id FK, order_date, qty, status)
- **truck_master**(truck_id PK, type, capacity, cost)
- **allocation**(date, truck_id FK, product_id FK, qty)

## 4. バッチ処理フロー
1. ETL実行 → DWH更新
2. 発注計算モジュール起動
3. 車両最適化モジュール起動
4. 結果をDBに保存し、ダッシュボードに通知

## 5. エラーハンドリング
- 各モジュールは例外発生時にログを残し、再実行可能なステータスを設定。
- 致命的なエラーはメールで管理者に通知し、運用手順に従って再実行。

## 6. パフォーマンス要件
- 発注計算から車両最適化まで、週次実行は2時間以内、日次調整は30分以内を目標とする。
- DBアクセスはインデックス設計とバルク操作で性能を確保する。

